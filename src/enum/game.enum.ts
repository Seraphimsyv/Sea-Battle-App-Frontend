export enum EnumShipStatus {
  Destroyed, Damaged, Full
}

export enum EnumPlayerStatus {
  NotConnect, Preparation, Ready, InGame
}

export enum EnumGameStatus {
  Waiting, Preparation, InGame, Finish
}